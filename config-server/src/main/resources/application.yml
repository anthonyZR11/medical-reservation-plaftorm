server:
  port: 8000
spring:
  cloud:
    config:
      server:
        git:
          default-label: feature/circuit-breaker
          uri: https://github.com/anthonyZR11/medical-reservation-plaftorm.git
          search-paths: config-data
  application:
    name: config-server